// Solicitar al usuario ingresar un string con palabras separadas por comas
const string = prompt("Ingrese una secuencia de caracteres con palabras separadas por comas:");

// Convertir el string en un array
const arrayString = string.split(",");

// Mostrar el resultado en la consola
console.log(arrayString);
